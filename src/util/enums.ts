export enum Language {
    C,
    CPP,
    JAVA,
    SHELL,
    PYTHON,
    JAVASCRIPT,
    TYPESCRIPT
}

export enum SortOptionType {
    TITLE,
    UPDATED,
    LANGUAGE
}

export enum SortOrderType {
    ASC,
    DESC
}

export enum ExecutionStatus {
    SUCCESS,
    FAILED
}