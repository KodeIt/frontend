/**
 * 
 * @param {string} title Add title to be set to the tab
 * 
 * @description This function is used to set the title of the tab.
 */
 export const useChangeTitle = (title:string) => {
    document.title = title;
}